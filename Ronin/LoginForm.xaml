<controls:MetroWindow x:Class="Ronin.LoginForm"
                      controls:ButtonHelper.PreserveTextCase="True"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Ronin"
        xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        TitleCharacterCasing="Normal"
        Title="Authentification" Height="140.876" Width="252.384" Closing="MetroWindow_Closing">
    <Border BorderThickness="1" BorderBrush="Black">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                
                <TextBox Margin="5" Name="keyTb" FontSize="14"></TextBox>
            
                <Button Margin="0,10,0,0" Width="100" FontSize="14" Click="Activate_Click" >Activate</Button>
            </StackPanel>
        </Grid>
    </Border>
</controls:MetroWindow>

